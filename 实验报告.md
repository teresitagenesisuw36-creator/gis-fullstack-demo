# 实验二：Web前端开发基础与GIS应用框架搭建

## 一、实验概述

### 1.1 实验目标
- 掌握 HTML5 语义化标签与 CSS3 布局（Flex/Grid）、响应式与主题，构建 GIS 应用界面骨架
- 掌握 JavaScript ES6+ 核心特性处理空间数据
- 掌握 Vue3 组合式 API 响应式、组件通信与生命周期钩子
- 掌握 Vue3 进阶（Pinia 状态管理、Vue Router 路由、Composables），搭建 GIS 单页应用

### 1.2 技术栈
- 构建工具：Vite 6
- UI 框架：Vue 3.5（组合式 API）
- 状态管理：Pinia 2 + pinia-plugin-persistedstate
- 路由：Vue Router 4
- 语言：JavaScript（ES6+）
- 样式方案：CSS3 变量 + Flexbox/Grid + 响应式布局

---

## 二、界面布局设计

### 2.1 布局结构图

```
┌──────────────────────────────────────────────────────┐
│                    TopBar（顶栏）                       │
│   [Logo/标题]   [地图] [数据] [关于]    [主题切换]     │
├────────────┬─────────────────────────────────────────┤
│  SideBar   │                                         │
│  (侧边栏)   │          MapArea（地图主区域）            │
│            │                                         │
│ ┌────────┐ │   ┌──────────────────────────────┐      │
│ │搜索过滤 │ │   │                              │      │
│ ├────────┤ │   │      模拟地图画布区域          │      │
│ │图层列表 │ │   │                              │      │
│ │☑ POI   │ │   │   ┌───┐  Toolbar（工具栏）    │      │
│ │☑ 道路  │ │   │   │ + │  ┌──────────┐        │      │
│ │☐ 行政  │ │   │   └───┘  │ 漫游     │        │      │
│ │        │ │   │          │ 放大     │        │      │
│ │        │ │   │          │ 缩小     │        │      │
│ │        │ │   │          │ 测量     │        │      │
│ └────────┘ │   │          │ 识别     │        │      │
│            │   │          └──────────┘        │      │
│            │   │  CoordDisplay（坐标显示）     │      │
│            │   │  经度: 116.xxxx  纬度: 39.xxxx│      │
├────────────┴─────────────────────────────────────────┤
│               StatusBar（状态栏）                      │
│  坐标: ...  │  图层: 2/3 可见  │  工具: 无 | WGS84   │
└──────────────────────────────────────────────────────┘
```

### 2.2 布局说明

| 区域 | 语义化标签 | 布局方式 | 功能 |
|------|-----------|---------|------|
| TopBar | `<header>` | Flexbox | 应用标题、导航菜单、主题切换 |
| SideBar | `<aside>` | Flexbox（纵向） | 图层搜索、图层列表控制 |
| MapArea | `<main>` | 相对定位 | 地图画布、工具栏、坐标显示 |
| StatusBar | `<footer>` | Flexbox | 实时坐标、图层统计、工具状态 |

### 2.3 CSS3 实现要点

- **Flexbox 布局**：整体采用 `flex-direction: column` 垂直布局，主区域水平 Flex
- **CSS 变量主题**：通过 `:root` 和 `[data-theme="dark"]` 定义两套色值，一键切换
- **过渡动画**：工具栏按钮 hover 时 `transform: translateX(-2px)` + `scale(1.2)`，主题切换 0.3s 过渡
- **响应式断点**：`@media (max-width: 768px)` 适配移动端，侧边栏折叠、工具栏缩略

---

## 三、组件结构图

```
App.vue
├── TopBar.vue                    # 顶栏 + 导航菜单 + 主题切换
├── SideBar.vue                   # 侧边栏容器
│   ├── LayerSearch.vue           # 图层搜索过滤（子组件）
│   └── LayerList.vue             # 图层列表（子组件，v-for + v-model）
├── <router-view>                 # 路由视图区域
│   ├── MapView.vue               # 地图页面
│   │   └── MapArea.vue           #   地图主区域
│   │       ├── MapToolbar.vue    #     地图工具栏
│   │       └── CoordinateDisplay.vue  # 坐标显示
│   ├── DataView.vue              # 数据管理页面
│   └── AboutView.vue             # 关于页面
└── StatusBar.vue                 # 底部状态栏
```

### 组件通信方式

| 通信方向 | 方式 | 示例 |
|----------|------|------|
| 父 → 子 | Props | `CoordinateDisplay` 接收 `coord` prop |
| 子 → 父 | Emit | `LayerList` emit `toggle-layer`、`set-opacity` |
| 跨组件 | Pinia Store | `mapStore` 管理鼠标坐标、主题、图层状态 |
| 逻辑复用 | Composables | `useLayers()`、`useTheme()` |

---

## 四、状态管理设计（Pinia）

### 4.1 mapStore 结构

```javascript
state: {
  center: [116.4074, 39.9042],   // 地图中心坐标
  zoom: 10,                       // 缩放级别
  mouseCoord: [0, 0],            // 鼠标实时坐标
  theme: 'light',                // 主题模式
  layers: [...],                 // 图层列表（对象数组）
  activeTool: null               // 当前激活工具
}

getters: {
  visibleLayers,                  // 可见图层
  visibleLayerCount,              // 可见图层数量
  isDark,                         // 是否深色主题
  layersByType,                   // 按类型分组
  formattedCoord                  // 格式化坐标字符串
}

actions: {
  setCenter(), setZoom(),         // 地图操作
  setMouseCoord(),                // 坐标更新
  toggleTheme(),                  // 主题切换
  toggleLayerVisibility(),        // 图层显隐
  setLayerOpacity(),              // 图层透明度
  addLayer(), removeLayer(),      // 图层增删
  setActiveTool(),                // 工具激活
  hydrateLayers()                 // 持久化恢复
}
```

### 4.2 状态持久化配置（加分项）

```javascript
persist: {
  key: 'gis-map-store',
  storage: localStorage,
  paths: ['theme', 'layers', 'center', 'zoom']
}
```

---

## 五、路由设计

| 路径 | 名称 | 组件 | 加载方式 | 权限 |
|------|------|------|----------|------|
| `/` | Map | MapView.vue | 懒加载 `() => import(...)` | 公开 |
| `/data` | Data | DataView.vue | 懒加载 | 公开 |
| `/about` | About | AboutView.vue | 懒加载 | 公开 |

- **路由守卫**：`router.beforeEach` 设置页面标题，检查 `meta.requiresAuth` 权限
- **懒加载**：3 个视图组件均使用动态 `import()`，构建后独立打包为 3 个 chunk

---

## 六、各模块实现说明

### 6.1 HTML5/CSS3 界面布局（15分）

**文件**：`TopBar.vue`、`SideBar.vue`、`StatusBar.vue`、`MapArea.vue`、`styles/variables.css`

- 语义化标签：`<header>`、`<aside>`、`<main>`、`<footer>`、`<nav>`
- CSS 变量：在 `variables.css` 定义 `--bg-primary`、`--text-primary`、`--accent-color` 等变量，深色主题通过 `[data-theme="dark"]` 覆盖
- 地图工具栏：5 个工具按钮，CSS3 `transition` 动画 + `:hover` 伪类效果
- 响应式：移动端侧边栏绝对定位可折叠，工具栏隐藏文字标签

### 6.2 JavaScript ES6+ 空间数据处理（30分）

**文件**：`modules/geoUtils.js`、`modules/geoJSONProcessor.js`、`models/LayerModel.js`

| 函数/类 | ES6+ 特性 | 功能 |
|---------|----------|------|
| `calcDistance()` | 箭头函数、解构参数 | Haversine 距离计算 |
| `formatCoordinate()` | 模板字符串、箭头函数 | 度分秒格式化 |
| `calcPolygonArea()` | 解构赋值 | Shoelace 面积计算 |
| `calcBoundingBox()` | 展开运算符 `...` | 边界框计算 |
| `loadGeoJSON()` | async/await | 异步加载 GeoJSON |
| `filterFeatures()` | 箭头函数、filter | 属性过滤 |
| `searchFeatures()` | 解构、filter、includes | 模糊搜索 |
| `countByType()` | reduce | 类型统计 |
| `calcDistancesTo()` | filter + map + sort 链式 | 距离排序 |
| `LayerModel` | class、私有字段 `#` | 图层数据模型封装 |

### 6.3 Vue3 组件化开发（25分）

**文件**：`CoordinateDisplay.vue`、`LayerList.vue`、`LayerSearch.vue`

- **组合式 API**：所有组件使用 `<script setup>` 语法
- **CoordinateDisplay**：Props 接收坐标，实时显示经纬度
- **LayerList**：`v-for` 渲染图层，`v-model` 复选框控制可见性，computed 统计过滤结果
- **LayerSearch**：`v-model` 双向绑定搜索关键词，`@input` 触发过滤，`@keydown.escape` 清除
- **父子通信**：LayerList 通过 `defineEmits` 向上发送 `toggle-layer`、`set-opacity`、`remove-layer` 事件
- **computed 统计**：`visibleLayerCount` getter 实时计算可见图层数

### 6.4 Vue3 进阶与应用集成（15分）

**文件**：`stores/mapStore.js`、`router/index.js`、`composables/useLayers.js`

- **Pinia**：`useMapStore` 管理全局地图/图层/主题/工具栏状态
- **Vue Router**：3 个页面路由，`createWebHistory` 历史模式
- **Composables**：
  - `useLayers()`：封装图层 CRUD 操作、搜索过滤、统计
  - `useTheme()`：封装主题切换逻辑
- **工具栏联动**：MapToolbar 调用 `store.setActiveTool()`，StatusBar 实时显示 `store.activeTool`

### 6.5 工程化增强（加分项 15分）

| 加分项 | 实现 | 文件 |
|--------|------|------|
| Pinia 持久化（+8分） | `pinia-plugin-persistedstate` 插件，localStorage 持久化主题/图层/地图状态 | `stores/mapStore.js` |
| 路由懒加载（+4分） | 3 个视图组件使用 `() => import(...)` 动态导入 | `router/index.js` |
| 路由权限控制（+3分） | `router.beforeEach` 导航守卫，检查 `meta.requiresAuth` | `router/index.js` |

### 6.6 AI 辅助重构说明

在开发过程中，使用 AI 辅助完成了以下改进：

1. **LayerModel 类优化**：AI 建议使用 ES2022 私有字段 `#visible` 和 `#opacity` 替代约定式 `_` 前缀，更安全地封装内部状态
2. **Composables 抽取**：AI 建议将图层和主题逻辑从组件中抽取为 `useLayers()` 和 `useTheme()`，减少代码重复，提升可测试性
3. **路由懒加载**：AI 建议三个视图组件全部采用动态 import，构建产物独立分 chunk，减小首屏加载体积

---

## 七、运行方式

```bash
# 安装依赖
npm install

# 启动开发服务器
npm run dev

# 构建生产版本
npm run build

# 预览生产构建
npm run preview
```

---

## 八、项目文件结构

```
gis-app/
├── index.html                     # HTML 入口
├── package.json                   # 依赖配置
├── vite.config.js                 # Vite 配置（别名 @）
├── public/
│   └── data/
│       └── sample.geojson         # 示例 GeoJSON 数据
└── src/
    ├── main.js                    # 应用入口（Pinia + Router 注册）
    ├── App.vue                    # 根组件（布局骨架）
    ├── router/
    │   └── index.js               # Vue Router 配置（懒加载 + 守卫）
    ├── stores/
    │   └── mapStore.js            # Pinia Store（含持久化）
    ├── composables/
    │   └── useLayers.js           # 图层逻辑复用（useLayers + useTheme）
    ├── modules/
    │   ├── geoUtils.js            # GIS 工具函数
    │   └── geoJSONProcessor.js    # GeoJSON 处理模块
    ├── models/
    │   └── LayerModel.js          # 图层数据模型（ES6 Class）
    ├── styles/
    │   └── variables.css          # CSS 变量 + 主题 + 全局样式
    ├── components/
    │   ├── TopBar.vue             # 顶栏组件
    │   ├── SideBar.vue            # 侧边栏组件
    │   ├── MapArea.vue            # 地图区域组件
    │   ├── StatusBar.vue          # 状态栏组件
    │   ├── CoordinateDisplay.vue  # 坐标显示组件
    │   ├── LayerList.vue          # 图层列表组件
    │   ├── MapToolbar.vue         # 地图工具栏组件
    │   └── LayerSearch.vue        # 图层搜索组件
    └── views/
        ├── MapView.vue            # 地图页面
        ├── DataView.vue           # 数据管理页面
        └── AboutView.vue          # 关于页面
```